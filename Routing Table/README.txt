Code written using Eclipse and Java v.13

Project was to make a program which the user can input a routing table, as well as the size of routing table
and an ip to compare to the table. The program then says which router the input ip will be sent to based on
the routing table entered by the user.

ip in format "xxx.xxx.xxx.xxx"
